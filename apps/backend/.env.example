# PostgreSQL Configuration
POSTGRES_HOST="localhost"
POSTGRES_PORT="5432"
POSTGRES_DATABASE="postgres"
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="postgres_password"

#DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DATABASE}?schema=public

DATABASE_URL="postgres://admin:admin@localhost:5432/postgres"

# AuthSCH Configuration
# Register your application at https://auth.sch.bme.hu/console/create
AUTHSCH_CLIENT_ID=authsch_client_id
AUTHSCH_CLIENT_SECRET=authsch_client_secret

# JWT Configuration
JWT_SECRET=jwt_secret

# Application
PORT=3001
FRONTEND_URL=http://localhost:3000